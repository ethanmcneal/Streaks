import styled from "styled-components";
import { CARD_COLOR, INV_TEXT_COLOR } from "./styles";

const CardContainer = styled.div`
background: ${CARD_COLOR};
color: ${INV_TEXT_COLOR};
margin: 20px;
text-align: center;
`

export default CardContainer
